# pathFinding_algorithm
This Go project simulates 2D automaton pathfinding in an environment with obstacles. It uses a custom stack for path tracking and implements a search algorithm with a Manhattan distance heuristic to guide automatons, enabling them to navigate while avoiding collisions. Commands allow environment setup, obstacle placement, and path generation
